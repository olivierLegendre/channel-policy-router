name: CI

on:
  push:
  pull_request:

jobs:
  unit:
    runs-on: ubuntu-latest
    env:
      CHANNEL_POLICY_ROUTER_PERSISTENCE_BACKEND: in_memory
      CHANNEL_POLICY_ROUTER_POSTGRES_AUTO_INIT: "false"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Export OpenAPI
        run: python scripts/export_openapi.py

      - name: Lint
        run: ruff check .

      - name: Type check
        run: mypy src

      - name: Unit tests
        run: pytest -m "not postgres_integration"

  postgres_integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: channel_policy_router
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d channel_policy_router"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      CHANNEL_POLICY_ROUTER_TEST_POSTGRES_DSN: postgresql://postgres:postgres@localhost:5432/channel_policy_router

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Postgres integration tests
        run: pytest -m postgres_integration
